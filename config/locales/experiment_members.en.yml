en:
  experiment_members:
    owner_only: Experiment owners only

    index:
      title:   Experiment Members
      info:    "The list of all groups and users having access to the experiment"
      delete:  "Delete"

      add_user:
        title:    "Add a User"
        label:    "User Name"
        hint:     "user"
        submit:   "Add User"

      add_group:
        title:    "Add a Group"
        label:    "Group Name"
        hint:     "group:group"
        submit:   "Add Group"

    create:
      success: "Record created"
      failure: "Failed to create the record"

    destroy:
      success: "Record deleted"
      failure: "Failed to delete the record"
