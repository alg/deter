- @submenu = :projects
.page-header
  %h1= raw t '.title'
  = raw t '.info'

.row
  .col-sm-12
    %table.table.projects
      %thead
        %tr
          %th= t '.project_id'
          %th.col-sm-2= t '.owner'
          %th.col-sm-2.text-right= t '.actions'
      %tbody
        - if @projects.blank?
          %tr
            %td{ colspan: 3 }= t '.nothing'
        - else
          - @projects.each do |p|
            %tr
              %td= link_to p.project_id, project_path(p.project_id)
              %td= p.owner
              %td.text-right
                = link_to t(".more"), "#", data: { pid: p.project_id }, class: 'more'
                = link_to t(".less"), "#", data: { pid: p.project_id }, class: 'less hide'
            %tr{ data: { more_pid: p.project_id }, class: 'hide' }
              %td{ colspan: 3 }
                .data-cell
                %p= link_to t(".delete"), project_path(p.project_id), method: :delete, data: { confirm: t(".confirm_delete") }, class: 'btn btn-xs btn-danger'

.row
  .col-sm-12
    = link_to t(".new_project"), :new_project, class: "btn btn-default"
    = link_to t(".join_project"), :new_project_join, class: "btn btn-default"
