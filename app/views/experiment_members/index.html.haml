- @submenu = :my_experiments
.page-header
  %h1= raw t '.title'
  = raw t '.info'

.row
  .col-sm-12
    %p TBD

.row
  .col-sm-12
    = link_to t('.save'), experiment_path(params[:experiment_id]), class: 'btn btn-default'
    = link_to t('.cancel'), experiment_path(params[:experiment_id]), class: 'btn btn-default'
