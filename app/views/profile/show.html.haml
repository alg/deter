.page-header
  %h1= raw t '.title'

= raw t '.info'

.row
  .col-sm-6
    %table.table.table-striped
      - ProfileFields::DISPLAY_FIELDS.each do |f|
        %tr
          %th= t ".#{f}"
          %td= @profile[f]
